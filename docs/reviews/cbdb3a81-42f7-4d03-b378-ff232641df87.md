# Review cbdb3a81-42f7-4d03-b378-ff232641df87

- **Task**: 7beee652-ad45-4931-b68d-8bd99489107f — Stop Hook liveness root fix
- **Level**: second-model
- **Reviewers**: independent `spec_review` and `standards_review` agents
- **Reviewed at**: 2026-07-21T16:32:19.462Z
- **Scope**: working tree on `codex/stop-hook-liveness-root-fix` after fresh runtime/E2E evidence
- **Final findings**: 0 blocking, 1 advisory

## Blocking findings resolved during review

### Prepared and commit-boundary repository races

The first implementation fingerprinted repository content only around criterion execution. Reviewers demonstrated direct-write windows before runner startup and between the runner's after-snapshot and commit.

**Resolution**: preparation now binds a full content fingerprint, execution compares before/after snapshots, commit takes another full snapshot outside `.task.lock`, and a bounded membership/stat/recent-content validation runs inside the final task-lock transaction. All snapshots share the operation's absolute deadline.

### Timeout did not prove descendant termination

The original child timeout could return after killing the direct child while a descendant survived with inherited pipes.

**Resolution**: the criterion runner owns a POSIX process group or Windows process tree and hard-terminates the entire tree. The regression records a descendant PID and proves it is gone before return.

### Side-effect evidence was lost on a dual race

A direct ignored-file write plus a concurrent `suspend` changed both repository content and authority. The authority mismatch returned before mutation evidence could advance artifact revision.

**Resolution**: stale verdicts remain discarded, but observed side effects are retained only on the same active/suspended task. The combined-race regression proves lifecycle remains suspended, artifact revision advances, review freshness expires, and round count does not change.

### Lock publication and filesystem portability

`mkdir` followed by `owner.json` exposed an ownerless live-acquire window; the first atomic-claim fix then assumed hard-link support.

**Resolution**: a complete sibling claim is published before the directory. Unsupported hard links fall back to `COPYFILE_EXCL`; incomplete fallback claims retain the stale-age grace. Paused-live, dead-owner, and fallback regressions pass.

### E2E harness liveness, freshness, and evidence claims

Review found unbounded app-server/owner waits, reusable preserved fixtures, and a report that counted app-server protocol evidence as the required visible Codex App UI proof.

**Resolution**: asynchronous runners use bounded structured process handles with whole-tree cleanup in `finally`; fixture runners require an explicit manifest-matching run ID and one-shot claim. The report now marks SHL-E2E-003 BLOCKED and labels app-server results as supporting evidence only.

## Advisory

### A1 — report directory predates the latest preserved run

The versioned report directory retains its original `2026-07-21-222647` name while the report body records the latest fresh run and its lineage as `2026-07-22-002112`.

**Disposition**: accepted for this cumulative review artifact because `prior_run_id` makes lineage explicit. Future fresh executions should create a timestamp-matching report directory.

## Verification observed by reviewers

- Product/spec review: 0 blocking findings, GO.
- Standards/report review: 0 blocking findings, GO.
- Behavioral suite: 145/145 passed.
- Remaining local suites: 222 passed, 9 Windows-only skipped, 0 failed.
- Fresh installed runtime: `8589aab54dd4`; installer dry-run: 0 warnings, 24 checks OK.
- Fresh Codex CLI probe: ten Stop turns plus the next turn, no criterion start or proof mutation.
- Supporting app-server protocol probe: ten Stop turns plus the next turn, no mutation; not treated as visible UI proof.
- Direct Claude-profile contracts and explicit-proof lock-isolation probe passed; real Claude sessions remain blocked by missing authentication.

## Explicit release blockers outside code review

- SHL-E2E-003 still needs a user-operated visible Codex App task because Computer Use is prohibited from controlling Codex.
- SHL-E2E-004 through SHL-E2E-007 still need an authenticated Claude model session.
- The final pushed commit still needs its full GitHub Actions matrix.
