# 切片 7:平台语义规格书

日期:2026-07-25
状态:规格先行,写定于实现之前。

上承:[WN 族场景](../scenarios/09-windows.md)(5 保留 / 1 出局)。
完成判据:WN-02 / 04 / 05 全绿(WN-01 / 06 见 §5),前六个切片不回归。

## 1. 写规格前先找到了一个缺陷

`src/domain/graph.mjs` 的跨循环重叠判断硬写了 `/`:

```js
left.startsWith(`${right}/`) || right.startsWith(`${left}/`)
```

而 claim 的身份由 `claimIdentity` 构造,用的是 `path.sep`——**Windows 上是 `\`**。
于是循环 A claim `src`、循环 B claim `src\nested`,`"src\\nested".startsWith("src/")`
为假,**两个循环拥有互相包含的路径而无人拦下**。CC-02 的不变式在 Windows 上是坏的。

CI 一直在 windows-2025 上跑,没抓到,因为现有的 CC-02 测试只覆盖了**相等**与 `.`
两种重叠——**相等在任何分隔符下都成立**。嵌套那一支从来没在 Windows 上被执行过。

顺带一记:`src/domain/receipt.mjs` 里两处形似的比较**是对的**,因为它们比的是 git
的路径,而 git 无论平台一律用 `/`。看起来一样的两段代码,正确性依据完全不同——
这正是"重复代码"这个气味真正危险的地方:**复制时对的那份和错的那份长得一模一样**。

裁决〔评审后加深〕:**日志只存一种路径形式,就是 `/`**;平台分隔符只在真正碰
文件系统时出现。

初版只把**比较**改成了分隔符感知,而**存进日志的形式仍然是平台相关的**。于是同一条
逻辑 claim 在两个平台上 **digest 不同**(切片 5 §3 明令禁止的那件事,我当时只想到了
排序),而一个在 Windows 上写下的 store,到 Linux 上**整个失去重叠检测**——同一个
缺陷,只是推迟到有人共享这个仓库的那天。已实测复现。

顺带一记:`src/domain/receipt.mjs` 里两处形似的比较**是对的**,因为它们比的是 git
的路径,而 git 无论平台一律用 `/`。看起来一样的两段代码,正确性依据完全不同——
这正是"重复代码"这个气味真正危险的地方。统一成 `/` 之后,两边**由构造一致**,
而不再是碰巧一致。

另需两个判断,不能合成一个:`pathContains` 是**对称**的(两条 claim 是否重叠),
`isUnder` 是**有向**的(某路径是否落在控制面之内)。用对称的那个做排除,会因为
"." 与一切重叠而把整棵树排掉——实现时就是这么错的。

## 2. 路径变体(WN-02)

不变式:**路径里有什么字符,不改变任何语义**。

- 含空格与 CJK 的根:开单、观测、收据、重放全流程可用;
- 创建与快照过程**不留下中间文件**(`.partial` 之类);
- win32:盘符大小写变体等价(引用 CC-03 的物理身份);junction 作为目录别名等价
  (引用 WT-02)。

前两条在三平台都可测,后两条只在 win32 有意义——CI 在 windows-2025 上跑,所以
它们不是纸面断言。

## 3. 判据超时:树杀与它的降级路径(WN-04)

POSIX 侧已定版(切片 2 §4):spawn 时 `detached`,超时杀负 pid,即整个进程组。
SL-09 用一个真的孙子进程压着它。

win32 没有进程组。机制是 `taskkill /pid N /T /F`。

### 3.1 降级路径能做到什么,不能做到什么〔本切片定版〕

场景要求"首次树杀失败后,降级路径仍在限时内消灭父进程与孙进程"。**诚实地说,
第二句做不到全部**:

- `taskkill` 失败最常见的原因是进程已经没了——此时无事可做,也无需做;
- 若 `taskkill` 本身不可用(PATH 被剥离),Node 没有任何内建手段触达孙进程。
  Windows 上等价于进程组的东西是 **job object**,而 Node 不暴露它。

裁决:降级路径**先重试一次树杀**(瞬时失败是常见情形),再退到杀直接子进程;
并且**在执行记录里说明孙进程可能存活**,而不是假装树死了。

〔评审后修订〕初版这条在它唯一针对的平台上不成立:`spawn` 遇到不存在的可执行文件
**不会同步抛错**,错误是异步到达的,而实现用 try/catch 包着 spawn——于是 Windows
上每一次树杀都被报成成功,**包括降级路径存在的唯一理由那一种**。现在结果经回调
报告,等杀手真的说了话之后才算数。

同一轮还发现:那个"孙进程可能存活"的事实**没有任何消费者**,只写进了 `execution.error`
而无人读。**一个运行时只对自己说的事实不是披露**。现在并入 summary,那是真正会落账、
也真正会被人看到的字段。

平台与 spawn 都做成可注入:**只能在有这个 bug 的机器上执行的分支,是没有人检查的
分支**。Windows 分支现在在三平台都被测试真正执行。

**"限时内返回"是可以保证的,并且必须保证**——超时的意义就是不挂死。这一条不打折。

**Flip 条件**:出现可用的 job object 绑定(原生模块或 Node 新 API),降级路径
应当改为绑定作业对象,这条限制随之消失。

## 4. 判据租约(WN-05)

"持有者 pid 已死但 deadline 未到 → 不抢占;deadline 已过 → 回收。"

机制在切片 1 已定版并已测(`locks.test.mjs`),而且**与锁类无关**——本切片补上
判据锁那一类的断言,因为一条只在别的锁类上被证过的性质,不能算这一类也被证过。

语义平台无关,但**进程存活探测在 win32 上语义不同**,所以这条必须在 Windows CI 上
真的跑,而不只是在开发机上绿。

## 5. WN-01 与 WN-06 不做

两条都以安装器为前提(三壳可调用、安装锁收割),而安装器已挂 D-05:绿地还没有
定义发行形态。它们随 D-05 一并回来。

## 6. 修订记录

- 2026-07-25(实现后):WN-02 / 04 / 05 全绿(`tests/platform.test.mjs`);全量
  200/200。降级路径实现时多加一条:POSIX 上 `ESRCH`(进程组本来就没了)不算
  "杀不干净"——把唯一无需过问的结局报成可疑,等于喊狼来了。
  **非空验证的一处诚实说明**:§1 那条缺陷的修复,**在 macOS 上无法用变异证伪**
  ——本机 `path.sep` 就是 `/`,把比较改回硬编码斜杠,测试照样全绿。已直接演示了
  失效形态(`pathContains("src\\nested", "src")` 在斜杠比较下为假),而那条测试
  用 `path.join` 构造嵌套路径,**它是 Windows CI 上的守卫,不是本机的**。
- 2026-07-25(代码评审后):四条。日志存平台相关的路径形式(§1,比初版更深一层
  的同一个缺陷);Windows 树杀的失败从来检测不到(§3.1);"孙进程可能存活"没有
  任何消费者;**WN-04 当时根本没有测试,而我在上一条修订记录里写了它"全绿"**——
  已补,并把 Windows 分支做成可注入,三平台都真正执行。全量 203/203。
  **非空验证**:四条中两条可在本机证伪(重试、有向排除各 1 / 5 项变红);
  "杀手起不来"改成可注入之后也能证伪;**"日志存平台形式"这条在本机无法证伪**
  ——`canonicalPath` 在 POSIX 上本该是恒等的(反斜杠是合法文件名字符),所以连
  单元断言都只能压住往返契约,转换本身仍然只有 Windows CI 能抓。
- 2026-07-25:本文写定于实现之前。写规格前发现并确认了 §1 的缺陷。
